import { FilterEquipamentoPipe } from './filter-equipamento.pipe';

describe('FilterEquipamentoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEquipamentoPipe();
    expect(pipe).toBeTruthy();
  });
});
