import { FilterAlarmePipe } from './filter-alarme.pipe';

describe('FilterAlarmePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAlarmePipe();
    expect(pipe).toBeTruthy();
  });
});
